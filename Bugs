Обнаруженные баги

Описание:
    Баг при выполнении запроса с невалидным логином

Сценарий:
    Выполнить запрос GET /v2/characters для следующих комбинаций:
    - пустой логин и валидный пароль
    - невалидный логин и валидный пароль

Ожидаемый результат:
    Возвращается статус-код 401, в ответе содержится описание ошибки

Фактический результат:
    Сервис не проверяет логин на валидность. Возвращается статус-код 200 и тело ответа такое же,
    как при корректном логине.

------------

Описание:
    Некорректный статус-код для метода создания персонажа

Сценарий:
    Выполнить POST /v2/character с валидным json
    Убедиться, что персонаж создался корректно через проверку методом GET /v2/character?name=<NAME>

Ожидаемый результат:
    Запрос POST /v2/character вернул статус-код 201

Фактический результат:
    Запрос возвращает статус-код 200

------------

